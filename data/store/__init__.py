"""Storage backends — SQLite cache for structured data, vector DB for text.

`data.store.sqlite` (Step 3) backs the structured cache. `data.store.vector`
and `data.store.llm_cache` are placeholders for the LLM/RAG layer.
"""
